c
#include<iostream.h>
#include<stdio.h>

int main()
{
    int i,int array[50], int find;
    printf("Enter the item to search" );
    scanf("%d",find);
    for(i=0;i<50;i++)
    {
        find==array[i];
        printf("%d Found Item is:, find);
    }
    else
    {
        printf("The item does not exist in an array");
    }
}